import React from "react";

/**
 * Компонент PomodoroLogo — SVG-иллюстрация помидора, используемая как логотип приложения Pomodoro.
 *
 * Ключевые характеристики:
 * - SVG-логотип масштабируется по пропсу `size` (по умолчанию 28px), обеспечивая гибкость в использовании.
 * - Используются простые SVG-примитивы для рисования:
 *   - `circle` для тела помидора с основным цветом и обводкой.
 *   - `path` для стилизованного хвостика/листьев, придающего узнаваемость.
 *   - дополнительный `circle` для блика, создающего ощущение объема.
 *
 * Цветовая схема:
 * - Основана на цветах TailwindCSS (`#EF4444`, `#B91C1C`, `#15803D`, `#166534`, `#FCA5A5`), что упрощает визуальное согласование с остальной частью интерфейса.
 *
 * Преимущества подхода:
 * - SVG обеспечивает идеальную масштабируемость без потери качества на любых устройствах и экранах.
 * - Использование React-компонента позволяет легко изменять размер через пропсы и переиспользовать в любом месте приложения.
 * - Изолированная реализация SVG исключает зависимость от сторонних изображений.
 *
 * Возможные расширения:
 * - Поддержка разных тем (например, темный/светлый фон).
 * - Анимация блика или листьев для придания интерактивности.
 * - При необходимости можно заменить SVG на спрайт или inline иконку без потери производительности.
 *
 * Заключение:
 * - `PomodoroLogo` — лёгкий и гибкий компонент для брендирования интерфейса, легко расширяемый под визуальные задачи.
 */
const PomodoroLogo = ({ size = 28 }: { size?: number }) => (
    <svg
        viewBox="0 0 64 64"
        xmlns="http://www.w3.org/2000/svg"
        width={size}
        height={size}
        fill="none"
    >
        {/* Тело помидора */}
        <circle cx="32" cy="36" r="20" fill="#EF4444" stroke="#B91C1C" strokeWidth="2" />

        {/* Хвостик/листочки */}
        <path
            d="M32 12
         C30 6, 34 6, 32 12
         M32 12
         C26 10, 26 14, 32 16
         C38 14, 38 10, 32 12"
            fill="#15803D"
            stroke="#166534"
            strokeWidth="2"
        />

        {/* Блик */}
        <circle cx="24" cy="28" r="4" fill="#FCA5A5" opacity="0.7" />
    </svg>
);

export default PomodoroLogo;
