import * as React from 'react';
import {cn} from "@/libs/utils";
import { HiUser } from "react-icons/hi";

type Props = {

};

/**
 * Компонент HeaderProfile — выводит информацию о пользователе в шапке интерфейса.
 *
 * Назначение:
 * - Отображение пользовательской иконки (через `HiUser`) и статического текста "Пользователь".
 * - Используется в верхней панели для обозначения пользовательского профиля или действий, связанных с пользователем.
 *
 * Визуальные особенности:
 * - Flex-контейнер выравнивает иконку и текст по горизонтали.
 * - `gap-2` создаёт равномерный отступ между иконкой и подписью.
 * - Цвет текста задается через `text-gray-600`, обеспечивая нейтральный, не доминирующий стиль.
 * - Иконка `HiUser` отображается размером 24px, что обеспечивает хорошую читаемость.
 *
 * Текущие ограничения:
 * - Компонент статичен: отображается фиксированное имя пользователя без интерактивности.
 *
 * Возможные улучшения:
 * - Можно добавить выпадающее меню с действиями (выход, настройки профиля и т.д.).
 * - Можно заменить статический текст динамическими данными с использованием состояния или стора.
 * - Можно сделать компонент кликабельным для перехода на страницу профиля.
 * - Возможность отображения аватара пользователя вместо стандартной иконки.
 *
 * Заключение:
 * - `HeaderProfile` — чистый, легко поддерживаемый компонент с базовым функционалом и понятной структурой.
 * - Подходит как заготовка для дальнейшего развития функциональности профиля.
 */
const HeaderProfile = (props: Props) => {
    return (
        <div className={cn("flex items-center gap-2 text-gray-600")}>
            <HiUser size={24} />
            <span>Пользователь</span>
        </div>
    );
};

export default HeaderProfile;