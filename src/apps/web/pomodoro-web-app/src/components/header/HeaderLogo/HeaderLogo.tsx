import * as React from 'react';
import {cn} from "@/libs/utils";
import PomodoroLogo from "@/components/images/PomodoroLogo/PomodoroLogo";

type Props = {
    
};

/**
 * Компонент HeaderLogo — отвечает за отображение логотипа и названия приложения в шапке интерфейса.
 *
 * Ключевые характеристики:
 * - Использует компонент `PomodoroLogo` для отображения графического логотипа (SVG или изображение).
 * - Название приложения выводится в виде простого текстового элемента (`<p>` с текстом "Pomodoro").
 *
 * Особенности верстки:
 * - Flexbox (`flex flex-row`) располагает логотип и текст горизонтально.
 * - `gap-2` добавляет небольшой отступ между логотипом и текстом для визуальной читаемости.
 * - `justify-center` и `items-center` выравнивают контент по центру как по горизонтали, так и по вертикали.
 *
 * Преимущества:
 * - Минимальная и чистая структура, легко расширяется (например, можно добавить ссылку, анимацию или слоган).
 * - Использование утилитарных классов Tailwind обеспечивает простую настройку внешнего вида.
 *
 * Возможные улучшения:
 * - Можно сделать компонент кликабельным (например, обернуть в ссылку на главную страницу).
 * - Добавить динамическое имя приложения через пропсы, если планируется использовать переиспользуемость.
 * - Адаптировать стили под разные темы (тёмная/светлая) при необходимости.
 *
 * Итог:
 * - Компонент выполняет свою задачу — компоновка логотипа и названия в шапке приложения с возможностью быстрой стилизации и масштабирования.
 */
const HeaderLogo = (props: Props) => {
    return (
        <div className={cn("flex flex-row gap-2 justify-center items-center")}>
            <PomodoroLogo />
            <p>Pomodoro</p>
        </div>
    );
};

export default HeaderLogo;